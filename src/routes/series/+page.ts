/** @type {import('./$types').LayoutLoad} */
export async function load(/*{ session }*/) {
	/*if (!session?.user) {
		return {
			status: 302,
			redirect: '/sign-in'
		};
	} else {
		return {
			props: {
				user: session.user
			}
		};
	}*/
}
